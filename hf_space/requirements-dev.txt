# Test deps — installed only by CI / locally for testing.
# Production deps live in requirements.txt.

-r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
httpx==0.27.2     # needed by fastapi.testclient

# Lint — gates PRs in CI (lint-hf-space job)
ruff==0.15.14
